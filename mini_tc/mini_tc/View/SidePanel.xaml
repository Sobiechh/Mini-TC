<UserControl x:Class="mini_tc.View.SidePanel"
             x:Name="SidePanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:mini_tc.View"
             xmlns:resx="clr-namespace:mini_tc.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel Margin="5">
        <StackPanel Margin="10" Orientation="Horizontal" HorizontalAlignment="Right">
            <Label Content="{Binding Source={x:Static resx:Resources.PathLabel}}"/>
            <TextBlock Text="{Binding CurrentPath,ElementName=PanelTCControl}"/>
        </StackPanel>
        <StackPanel Margin="10" Orientation="Horizontal" HorizontalAlignment="Right">
            <Label Content="{Binding Source={x:Static resx:Resources.DriveLabel}}"/>
            <ComboBox ItemsSource="{Binding AvailableDrives,ElementName=PanelTCControl}" SelectedItem="{Binding SelectedDrive,ElementName=PanelTCControl}"/>
        </StackPanel>
        <ListBox Height="500" SelectionMode="Single" ItemsSource="{Binding CurrentPathContent,ElementName=PanelTCControl}" SelectedItem="{Binding SelectedPath,ElementName=PanelTCControl,Mode=TwoWay}"/>
    </StackPanel>
</UserControl>
